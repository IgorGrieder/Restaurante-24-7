// Initial Data
startPage()
// Evets ---------------------------------------

// Functions -----------------------------------
function startPage () {
  const menuInnerHTML = menu.map((item) => {
    return `
    
    `
  })
  const display = menuInnerHTML.join('')
}
